[project]
name = "mozgoslav-searxng-sidecar"
version = "0.1.0"
description = "SearXNG self-hosted meta-search sidecar for Mozgoslav."
requires-python = ">=3.11"

[tool.renovate]
packageRules = [
  { matchPackageNames = ["searxng"], automerge = false }
]
