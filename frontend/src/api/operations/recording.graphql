subscription SubscriptionRecordingPartials($recordingId: UUID!) {
  recordingPartials(recordingId: $recordingId) {
    recordingId
    sessionId
    text
    startSeconds
    endSeconds
    isFinal
    observedAt
  }
}
