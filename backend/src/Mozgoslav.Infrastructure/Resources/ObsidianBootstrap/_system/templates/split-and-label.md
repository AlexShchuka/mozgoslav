<%*
// Хоткей: «Templater: Insert split-and-label» (НЕ Create!).
// Курсор в файле inbox, ничего не выделено.
// Output блока пустой → вставки нет → файл не меняется.
// JS создаёт атомарные файлы в ideas/insights/people/questions/tasks.
await tp.user.split_and_label(tp);
%>
