# renovate

Self-hosted or GitHub-hosted Renovate Bot opening weekly PRs for NuGet, npm, pip, and GitHub Actions dependencies. Config in `renovate.json`: group minor bumps by ecosystem, auto-merge patch updates once the CI is fully green, require manual review on majors. Replaces the scattered "bump when I remember" flow.
