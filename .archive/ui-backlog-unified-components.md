# unified components

Single source of truth for every primitive and composite in the UI. Audit every feature for hand-rolled buttons, inputs, dialogs, cards, and tables; replace with the shared components. Anything feature-local that reimplements a primitive is a bug, not a variation.
