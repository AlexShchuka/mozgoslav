# keychain-backed secrets

Move LLM and GitHub tokens out of SQLite into macOS Keychain via the native helper. Needs a new contract between backend and helper (today the helper only speaks dictation).
