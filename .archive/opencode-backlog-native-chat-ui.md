# native chat UI

Replace the terminal with a Mozgoslav-native chat surface. Blocked on OpenCode exposing a stable structured event protocol and on accepting the maintenance cost of tracking that protocol.
