# bugs axis per feature

Add `docs/features/<name>/bugs/<slug>.md` alongside `decisions/` and `backlog/` — one known issue per file, retired by moving to `.archive/` when fixed. Today bugs are either in GitHub Issues or mixed into backlog; a separate on-disk axis gives agents something to triage without leaving the repo.
