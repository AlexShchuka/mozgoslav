# style consistency pass

Sweep every feature's styled-components for off-token colours, one-off spacings, inconsistent radii and shadows. Everything comes from the shared theme tokens. No magic values outside the theme module.
