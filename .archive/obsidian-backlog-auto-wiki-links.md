# auto wiki-links to existing vault entries

When exporting a processed note, scan mentioned people and topics against the existing vault and wrap matches as `[[Person]]` / `[[Topic]]` wiki-links automatically. Avoid fragmentation when a name appears in multiple spellings — canonicalise against the vault index before linking.
