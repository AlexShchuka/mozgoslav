# profiles — backlog

- **glossary autosuggestion from previous notes.** Mine terms, suggest additions. Nice-to-have.
- **multilingual glossary.** Flat list today; needs a typed `{ lang: [terms] }` shape and a one-shot migration.
- **per-profile LLM override.** One active provider today. Profile-level `{ provider, model }` override resolvable by the LLM factory.
