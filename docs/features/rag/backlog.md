# rag — backlog

- **web-aware RAG.** Privacy-critical. Needs its own decision doc with privacy boundaries before any implementation.
- **reindex on note change.** Today manual button press. Could hook post-export to auto-reindex. Small UX win.
- **sidecar `process-all`.** Chain diarize → gender → emotion → NER → cleanup → merge in one round-trip. Optimisation, not new capability.
