# remove logs section from UI

End-user UI does not need a logs viewer. Remove the sidebar entry, the route, the feature folder and the frontend API client. Keep the backend log endpoint for diagnostics but stop surfacing it as an in-app page.
