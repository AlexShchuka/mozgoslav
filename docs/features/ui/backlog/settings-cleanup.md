# settings cleanup

Settings page is a long linear form today. Group by concern (recording, dictation, LLM, Obsidian, sync), collapse rarely-touched options, validate inline, drop unused fields that no feature reads. Keep one visible settings surface — not a tree of nested panels.
