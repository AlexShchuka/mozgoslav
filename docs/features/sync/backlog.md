# sync — backlog

- **phone pairing end-to-end validation.** REST/SSE + QR-pairing UI exist, never run against a real phone. Needs a handheld session.
- **conflict resolution UI.** Syncthing leaves `*.sync-conflict-*.md` on divergent edits. Today invisible in UI. Risk of silent loss exists; UI stays low priority while sync itself stays optional.
