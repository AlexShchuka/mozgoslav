# commitlint

Adopt Conventional Commits (`feat:`, `fix:`, `chore:`, `docs:`, …) enforced by `@commitlint/config-conventional` in a lefthook `commit-msg` hook. Mirror in CI for inbound PRs (GitHub Action that parses the title). Unlocks machine-generated changelog and semantic-release later without needing to rewrite history.
