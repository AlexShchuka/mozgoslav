# aggregated topic notes

Per-topic rolling note spanning all conversations on a given theme. Needs a stable topic extraction + canonicalisation step so the same topic does not fragment across near-synonyms.
