# Production dependencies for the Mozgoslav Python ML sidecar.
# The full stack (torch + transformers + librosa + …) weighs ~4 GB
# resolved; installing it happens at DMG build time on macOS and at
# first sidecar start on Linux dev boxes. The CI Dockerfile
# (``Dockerfile.test``) installs this file minus ``transformers``
# (audeering gender/emotion — Tier 2, not in CI).

# --- Web framework ---
fastapi==0.136.1
uvicorn[standard]==0.46.0
python-multipart==0.0.26
pydantic==2.13.3
pydantic-settings==2.14.0

# --- ML runtime ---
# torch pulls ~4GB of CUDA wheels on Linux by default — overkill for the
# sidecar which runs CPU-only. Pin Linux to the CPU-only index; macOS wheels
# are CPU-native already and resolve from PyPI. PEP 440 forbids `>=` with
# local version labels (`+cpu`), so Linux uses exact-pin.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0 ; sys_platform == 'darwin'
torch==2.11.0+cpu ; sys_platform == 'linux'
torchaudio==2.11.0 ; sys_platform == 'darwin'
torchaudio==2.11.0+cpu ; sys_platform == 'linux'
transformers==5.6.2
huggingface_hub==1.11.0
soundfile==0.13.1
librosa==0.11.0
numpy==2.4.4

# --- Diarization stack ---
# ``webrtcvad-wheels`` ships precompiled wheels for the resemblyzer's
# mandatory ``webrtcvad`` dependency — the original ``webrtcvad``
# package fails to build on Python 3.12 without a pre-installed C
# toolchain. The wheels package exposes the same ``webrtcvad`` import
# name, so no code change is needed.
silero-vad==6.2.1
resemblyzer==0.1.4
webrtcvad-wheels==2.0.14
scikit-learn==1.8.0

# --- NER ---
natasha==1.6.0

# --- Embeddings (ADR-005 RAG swap-in) ---
sentence-transformers==5.4.1
