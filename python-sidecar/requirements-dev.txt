# Lightweight dependencies for local scaffold verification.
# Enough to import the app, run uvicorn, and exercise the test suite
# without pulling the full ML stack.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0
pydantic-settings>=2.0
pytest>=8.0
httpx>=0.27
ruff>=0.5
black>=24.0
