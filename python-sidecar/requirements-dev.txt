# Dependencies for local verification and CI.
#
# The split between this file and ``requirements.txt`` is:
#
# * ``requirements.txt`` — everything a production sidecar needs,
#   including the big ``transformers`` install used by the audeering
#   Tier-2 endpoints (gender/emotion).
# * this file — the same set minus ``transformers``/``sentence-transformers``
#   so CI stays under three minutes. Tier-2 endpoints are exercised via
#   the ``ModelNotAvailableError`` path in unit tests; model loading is
#   covered by the C# integration test against the running sidecar.

# --- Web framework ---
fastapi==0.136.1
uvicorn[standard]==0.46.0
pydantic==2.13.3
pydantic-settings==2.14.0
httpx==0.28.1
python-multipart==0.0.26

# --- ML runtime (needed by Tier-1 diarize + ner) ---
# Plain ``torch`` CPU is fine — the diarize test uses a fake embedder
# so no actual inference runs in CI. PEP 440 forbids `>=` with local
# version labels, so the Linux pin is exact.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0 ; sys_platform == 'darwin'
torch==2.11.0+cpu ; sys_platform == 'linux'
torchaudio==2.11.0 ; sys_platform == 'darwin'
torchaudio==2.11.0+cpu ; sys_platform == 'linux'
numpy==2.4.4
librosa==0.11.0
soundfile==0.13.1

# --- Tier-1 services (always-available) ---
silero-vad==6.2.1
resemblyzer==0.1.4
webrtcvad-wheels==2.0.14
scikit-learn==1.8.0
natasha==1.6.0
# pymorphy2 (transitive via natasha) imports `pkg_resources` at module
# load time. setuptools>=81 removed that API, breaking NER tests.
# Keep setuptools on the last version that still ships pkg_resources
# until natasha/pymorphy2 upgrade.
setuptools<81

# --- Dev / test tooling ---
pytest==9.0.3
ruff==0.15.11
black==26.3.1
