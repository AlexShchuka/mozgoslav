"""Service layer: one class per ML task.

V3 services (diarize, gender, emotion, ner) are stubs in this scaffold
and MUST be replaced with real implementations before the sidecar ships.
The cleanup service is implemented for real because it is pure-Python
regex work and unlocks C# pipeline tests today.
"""
